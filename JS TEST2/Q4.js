book=()=>{
    let a=document.querySelector("#h1")
    let b=document.querySelector("#h2")
    let c=document.querySelector("#h3")
    a.innerHTML=Math.round(Math.random()*100)
    b.innerHTML=Math.round(Math.random()*100)
    c.innerHTML=Math.round(Math.random()*100)
}